var mm = require('./lib/index'),
    fs = require('fs');

var parser = new mm(fs.createReadStream("01 - The Apers - It's All Over You Know.mp3"));

parser.on('metadata', function(result) {
  console.log(result);
});

parser.on('done', function() {
  console.log("in done")
});